<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwillbs.mapper.BoardMapper">

	<insert id="insertMember">
		insert into member (id, pass, name, email,phone, salt, eno) 
		value (#{id}, #{pass}, #{name}, #{email}, #{phone}, #{salt}, #{eno})
	
	</insert>

	<insert id="insertKakaoMember">
		insert into member (id, pass, name, email, phone, salt, sns_email) 
		value (#{id}, #{pass}, #{name}, #{email}, #{phone}, #{salt}, #{sns_email} )
	
	</insert>
	
	<select id="getSalt" resultType="string">
		select salt from member 
		where id = #{id}
	
	</select>
	
	<select id="getMember" resultType="com.itwillbs.domain.MemberVO">
		select * from member 
		where id = #{id}
	
	</select>
	

</mapper>